export const API = 'http://mbapp.e-bash.today/mob_app';

export const userAuthorize = `${API}?action=pin`;
export const loginWithPin = `${API}?action=login`;
export const getUserInfo = `${API}?action=info`;
export const userUpdate = `${API}?action=update`;
export const specials = `${API}?action=promo`;
export const getAboutProgram = `${API}?action=about`;
export const getAboutCompany = `${API}?action=company`;
export const getBonuses = `${API}?action=bonuses`;
